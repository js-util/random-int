{
	"name": "@js-util/random-int",
	"version": "1.0.0",
	"description": "Returns a random value between max (exclusive) and min (inclusive)",

	"keywords": [
		"random",
		"int"
	],

	"main": "randomInt.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},

	"author": "picocreator",
	"license": "MIT",
	"private": false,

	"repository": {
		"type": "git",
		"url": "https://github.com/js-util/random-int.git"
	},
	
	"engines": {
		"node": ">=8.0.0"
	}
}
